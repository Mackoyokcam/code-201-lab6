'use strict';

var firstAndPike = {
  minCust: 23,
  maxCust: 65,
  avgCookie: 6.3,
  generateRandom: function() {
    return Math.floor(Math.random() * (this.maxCust - this.minCust + 1)) + this.minCust;
  },
  calculateCookie: function() {
    // Calculate cookie stuff here
  },
  results: [],
};

var seaTacAirport = {
  minCust: 23,
  maxCust: 65,
  avgCookie: 6.3,
  generateRandom: function() {
    return Math.floor(Math.random() * (this.maxCust - this.minCust + 1)) + this.minCust;
  },
  calculateCookie: function() {
    // Calculate cookie stuff here
  },
  results: [],
};

var seattleCenter = {
  minCust: 23,
  maxCust: 65,
  avgCookie: 6.3,
  generateRandom: function() {
    return Math.floor(Math.random() * (this.maxCust - this.minCust + 1)) + this.minCust;
  },
  calculateCookie: function() {
    // Calculate cookie stuff here
  },
  results: [],
};

var capitolHill = {
  minCust: 23,
  maxCust: 65,
  avgCookie: 6.3,
  generateRandom: function() {
    return Math.floor(Math.random() * (this.maxCust - this.minCust + 1)) + this.minCust;
  },
  calculateCookie: function() {
    // Calculate cookie stuff here
  },
  results: [],
};

var alki = {
  minCust: 23,
  maxCust: 65,
  avgCookie: 6.3,
  generateRandom: function() {
    return Math.floor(Math.random() * (this.maxCust - this.minCust + 1)) + this.minCust;
  },
  calculateCookie: function() {
    // Calculate cookie stuff here
  },
  results: [],
};

firstAndPike.calculateCookie();
seaTacAirport.calculateCookie();
seattleCenter.calculateCookie();
capitolHill.calculateCookie();
alki.calculateCookie();
